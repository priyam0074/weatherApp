import { CapitualizeFilterPipe } from './capitualize-filter.pipe';

describe('CapitualizeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitualizeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
